apiVersion: v1
kind: Service
metadata:
  name: mcp-service
  namespace: backend
  labels:
    app: mcp-service
spec:
  selector:
    app: mcp-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
